name: CI Infra Selftest

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  infra:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run INFRA selftest
        run: bash .opencode/workflow/selftest.sh --mode infra --wait 900
